services:
  - type: web
    name: ai-music-proxy
    runtime: python
    rootDir: proxy
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --timeout 600
    plan: free
